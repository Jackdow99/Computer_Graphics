# 배경이 바뀌는 룸 투어
> **React Three Fiber (R3F)**와 **Three.js**를 활용하여 구현하는 실시간 공간 전환형 3D 인터랙티브 웹 프로젝트 계획서입니다. 
> 고정된 실내 아키텍처 내에서 사용자의 조작에 따라 전역 씬 그래프를 동적으로 재구성하여 하나의 공간에서 4가지 이색적인 세계관을 탐색할 수 있도록 설계되었습니다.

---

## 1. 프로젝트 개요

* **프로젝트명**: 배경이 바뀌는 룸 투어
* **핵심 컨셉**: 단 한 번의 키보드 입력(`Spacebar`)으로 방 안의 인테리어와 틀은 유지한 채, 창밖의 풍경을 완전히 상반된 4가지 테마(**태양계 ➔ 바다 ➔ 숲 ➔ 연금술 공방**)로 순환 전환하여 사용자에게 극적인 공간적 해방감과 시각적 경험을 제공합니다.

---

## 2. 그래픽스 구성 요소별 상세 설계 (기존 코드 기반 초안)

### 1) 씬 구조 및 상태 관리 (Scene & State)
스페이스바 입력(`viewMode`)에 따라 렌더링 노드를 켜고 끄는 **조건부 렌더링**을 적용하여 GPU 오버헤드를 방지합니다.

* **공통 레이어 (`SolidRoom`)**: 사방이 막힌 실내 구조 및 대형 통유리창 프레임
* **배경 레이어 분기**:
  * **태양계 (`space`)**: 중심 태양과 6대 행성의 공전/자전 매트릭스 변환 씬
  * **바다 (`ocean`)**: 수중 안개 효과, 상승 기포, 물고기 떼가 구현된 심해 씬
  * **숲 (`forest`)**: 자연광 조명과 수목 메쉬가 무작위로 배치되는 자연 씬
  * **연금술 공방 (`workshop`)**: 아기자기한 판타지 소품과 마법 이펙트가 포함된 실내 씬

### 2) 카메라 시스템 (Camera System)
* **카메라 종류**: 깊이감을 현실적으로 표현하는 `PerspectiveCamera` 
* **초기 위치**: 방 내부와 창밖 원경을 동시에 인지할 수 있는 위치 고정

### 3) 메쉬 구성 요약 (Mesh: Geometry & Material)

### 3) 메쉬 구성 (Mesh)

| 테마 | 주요 오브젝트 | 구현 방식 |
| :--- | :--- | :--- |
| **공통 실내** | 방 내부, 통유리창 | `box`/`plane` 구조 및 투명 유리 질감 |
| **태양계** | 태양 및 6대 행성 | `sphereGeometry` 및 행성별 PBR 색상 매핑 |
| **바다** | 유영하는 물고기 떼 | `ExtrudeGeometry` 및 수중 메탈릭 질감 |
| **숲** | 나무, 연못 주변 바위와 잔디 | `cylinder`/ `coneGeometry` 조합의 로우폴리 배경 숲 구현 |
| **공방** | 마법 솥 및 도구 | 회전체 구조 및 신비로운 발광/반사 질감 |

### 4) 조명 시스템 (Lighting)
* **태양계**: 어두운 주변광 + 태양 중심의 강한 `pointLight` 
* **바다**: 푸른 주변광 + 상단 스포트라이트 
* **숲**: `directionalLight` 와 `fog`를 활용한 부드럽고 몽환적인 숲 분위기 연출
* **공방**: 은은한 실내 조명 + 마법 시약 등의 컬러 포인트 조명 
---

## 3. 기능 및 사용자 인터랙션 (Interactions & Features)

### 1) 환경 스위칭 시스템
* **순환 구조**: 스페이스바 입력 시 `space ➔ ocean ➔ forest ➔ workshop` 순으로 배경 무한 루프 전환
* **예외 처리**: 브라우저 기본 스크롤 방지를 위해 `event.preventDefault()` 적용

### 2) 카메라 및 조작 제한 (`OrbitControls`)
* **시선 및 회전 제한**: 방 중앙(`[0, 0.4, 0]`)에 시선을 고정하고 수직 회전각을 제한하여 화면 뒤집힘 방지
* **줌 범위 가두기**: `minDistance={0.5}`, `maxDistance={2.4}` 설정으로 카메라의 벽면·유리창 뚫기 차단

### 3) 오브젝트 실내 침범 방지
* **공간 격리**: 창밖 오브젝트들이 방 안으로 파고들지 못하도록 유리창 너머 안전 영역(`z <= -4.5`)에만 배치 및 유영 통제
