# 🌌 배경이 바뀌는 룸 투어 (The Switching Backgrounds Room Tour)

> **React Three Fiber (R3F)**와 **Three.js**를 활용하여 구현하는 실시간 공간 전환형 3D 인터랙티브 웹 프로젝트 계획서입니다. 
> 고정된 실내 아키텍처 내에서 사용자의 조작에 따라 전역 씬 그래프(Scene Graph)를 동적으로 재구성하여 하나의 공간에서 4가지 이색적인 세계관을 탐색할 수 있도록 설계되었습니다.

---

## 📌 1. 프로젝트 개요 (Overview)

* **프로젝트명**: 배경이 바뀌는 룸 투어
* **개발 환경**: React (v18+), Vite, Three.js, `@react-three/fiber`, `@react-three/drei`
* **핵심 컨셉**: 단 한 번의 키보드 입력(`Spacebar`)으로 방 안의 인테리어와 틀은 유지한 채, 창밖의 풍경을 완전히 상반된 4가지 테마(**태양계 ➔ 바다 ➔ 숲 ➔ 연금술 공방**)로 순환 전환하여 사용자에게 극적인 공간적 해방감과 시각적 경험을 제공합니다.

---

## 🛠️ 2. 그래픽스 구성 요소별 상세 설계 (Core Technical Design)

### 1) 씬 구조 및 상태 관리 (Scene & State)
리액트의 전역 상태(`viewMode`) 스위칭에 따라 불필요한 GPU 오버헤드를 방지하기 위해 렌더링 파이프라인의 **조건부 노드 활성화(Conditional Rendering)** 기법을 적용합니다.

* **공통 레이어 (`SolidRoom`)**: 사방이 불투명한 벽면 박스 구조체와 정면 대형 통유리창 프레임.
* **배경 레이어 분기**:
  * **태양계 (`space`)**: 중심 광원을 기준으로 한 행성들의 공전/자전 매트릭스 변환 씬.
  * **바다 (`ocean`)**: `Fog` 감쇠 모델과 상단 스포트라이트를 활용한 수중 씬.
  * **숲 (`forest`)**: 난수 연산(`Math.random`) 기반의 수목 배치 및 자연광 중심의 오가닉 씬.
  * **연금술 공방 (`workshop`)**: 아기자기한 판타지 오브젝트들과 다중 포인트 광원 중심의 실내 확장 씬.

### 2) 카메라 시스템 (Camera System)
* **카메라 종류**: `PerspectiveCamera` (원근 카메라)를 채택하여 실내외 공간의 깊이감과 왜곡 효과를 현실적으로 연출.
* **시야각 & 위치**: 기본 FOV 75도로 설정하며, 사용자가 방의 구조와 창문 밖 원경을 동시에 인지할 수 있는 최적의 앵글인 `position={[0, 0.4, 1.5]}`를 초기 좌표로 지정.

### 3) 메쉬 물리 구성 명세 (Mesh: Geometry & Material)

| 테마 분류 | 오브젝트 (Mesh) | 지오메트리 (Geometry) | 재질 (Material) & 시각 효과 |
| :--- | :--- | :--- | :--- |
| **공통 실내** | 방 벽면 및 바닥 | `boxGeometry`, `planeGeometry` | `meshStandardMaterial` (White, 무광 및 매끄러움 밸런싱) |
| **공통 실내** | 정면 통유리창 | `planeGeometry` | `meshStandardMaterial` (투명도 `opacity={0.08}`, 반사율 `metalness={0.9}`) |
| **🌌 태양계** | 태양 및 6대 행성 | `sphereGeometry` | `meshBasicMaterial`(태양 자체 발광), 수성~토성 특유의 PBR 색상 매핑 |
| **🐬 바다** | 유영하는 물고기 떼 | `ExtrudeGeometry` (2D Shape) | `meshStandardMaterial` (맑은 메탈릭 실버와 파스텔 블루 조화) |
| **🌳 숲** | 로우폴리 나무와 바위 | `cylinderGeometry`, `dodecahedronGeometry` | 지형 특성에 맞는 톤다운된 그린/브라운 계열 조화 |
| **🔮 공방** | 마법 솥 및 플라스크 | `latheGeometry`, `torusGeometry` | 신비로운 보라/초록 광택 재질 및 초자류 특유의 굴절·반사 질감 |

### 4) 조명 시스템 (Lighting System)
각 테마로 시점 전환이 이루어질 때 전역 조명의 컬러풀한 변화와 감쇠율을 제어하여 공간의 맥락을 형성합니다.
* **태양계**: 극단적인 명암 대비를 위해 낮은 주변광(`intensity={0.2}`) + 태양 중심의 강력한 `pointLight` 가동.
* **바다**: 사방이 푸르게 가득 차는 수중 광량(`ambientLight intensity={1.3}`) + 상단 스포트라이트.
* **숲**: 따스한 햇살 느낌의 주황빛 `directionalLight` + 옅은 녹색 계열의 서브 림 라이트 배치.
* **연금술 공방**: 전체적으로 어둡고 아늑한 실내 톤 + 신비로운 마법 시약 빛을 발산하는 형형색색의 복수 `pointLight` 연출.

---

## ⌨️ 3. 기능 및 사용자 인터랙션 (Interactions & Features)

### 1) 순환형 환경 스위칭 시스템 (Keyboard Loop Interaction)
* 사용자가 키보드의 `Spacebar` 키를 누르면 `useState` 상태가 일방향 순환 구조로 트리거됩니다.
* **루프 시퀀스**: `space` ➔ `ocean` ➔ `forest` ➔ `workshop` ➔ `space` (무한 순환)
* 입력 시 웹 브라우저의 기본 페이지 다운 동작을 방지하기 위해 `event.preventDefault()` 예외 처리를 기본 적용합니다.

### 2) 마우스 휠 및 조작 영역 가두기 (`OrbitControls` Optimization)
* **시선 축 고정**: 3D 공간의 회전 중심축(`target`)을 방의 정중앙인 `[0, 0.4, 0]`으로 강제 구속하여 마우스 조작 시 궤도가 이탈하지 않도록 통제합니다.
* **마우스 휠 줌 한계 설정**: 휠을 밀고 당길 때 정면 통유리를 파고들거나 뒷벽 바깥으로 카메라가 튕겨 나가는 클리핑 버그를 해결하기 위해 `minDistance={0.5}`, `maxDistance={2.4}` 락(Lock)을 설정합니다.
* **시각 회전각 제한**: 카메라가 천장과 바닥면을 과도하게 수직 횡단하여 화면이 뒤집히는 물리 현상을 막기 위해 피치(Pitch) 각도를 안전 범위로 통제합니다.

### 3) 예외 처리: 오브젝트 실내 침범 방지 알고리즘 (Object Isolation)
* 외부 배경 요소들(공전 행성, 물고기 떼, 수목 자원, 공방 가구 등)이 움직이거나 배치되는 과정에서 방 내부 영역으로 파고드는 현상을 물리적으로 완전 격리합니다.
* 정면 유리창 단면의 한계 좌표계인 `z = -3`을 기준으로, 모든 배경 테마 오브젝트들을 부모 그룹 `<group position={[0, 0, -10]}>` 또는 로컬 수학 공식을 통해 **최소 `z = -4.5`보다 깊은 안전지대(창밖 영역) 레이어 안에서만 유영 및 배치**되도록 차단벽 연산을 수행합니다.
